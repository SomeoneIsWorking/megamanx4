#pragma once

class Core;

namespace x4::native_overrides {

// Install projection and loading owners against the active authenticated executable image.
void install(Core &core);

} // namespace x4::native_overrides
